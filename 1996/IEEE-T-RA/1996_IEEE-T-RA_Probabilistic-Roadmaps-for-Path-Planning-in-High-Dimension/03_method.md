# Method - Probabilistic Roadmaps for Path Planning in High-Dimensional Configuration Spaces

> Canonical metadata: [01_overview.md](./01_overview.md).
> Evidence maturity: `FULL_TEXT_CHECKED`.
> Analysis basis: full-text PDF body checked on 2026-09-02 (15 pages; tesseract OCR fallback; extraction quality: high); canonical paper source: https://kavrakilab.rice.edu/publications/kavraki-svestka1996probabilistic-roadmaps-for.html; PDF retrieval source: https://kavrakilab.org/publications/kavraki-svestka1996probabilistic-roadmaps-for.pdf. The note is an evidence-anchored body analysis; exact tables/equations remain at the cited page anchors. Reading tracker status/evidence was not changed.

## Method in One Sentence

PDF body method statement (p. 7 (B. The Query Phase), p. 5 (6) N.- a set of candidate neighbors), p. 6 (6) N.- a set of candidate neighbors), p. 7 (B. The Query Phase), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS)): An example of a planner having the above property is the straight-line planner of Section IIT-A-1, If the local planner performs a certain deterministic) action when a collision is detected, ...

## Method Body Digest

- **p. 7 / B. The Query Phase - extractive body cue:** An example of a planner having the above property is the straight-line planner of Section IIT-A-1, If the local planner performs a certain deterministic) action ...
- **p. 5 / 6) N.- a set of candidate neighbors - extractive body cue:** Firs, discretize the line segment (more ‘generally, any path generated by the local planner) into a number of configurations ¢1,...,¢m such that for each pair ...
- **p. 6 / 6) N.- a set of candidate neighbors - extractive body cue:** Then We try to connect n tothe other connected components of the network in the same way as in the constuction step.
- **p. 7 / B. The Query Phase - extractive body cue:** The local planner is deterministic and it will produce the same path every time it is called with the same input configurations Collisions need not ...
- **p. 8 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** First use the algorithm in [37] to produce the vertices of the obstacles in the link's C-space.
- **p. 8 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** (This algorithm takes linear time in the number of vertices of the objects.) Then draw and fill the obstacles into the 2-D bitmap.
- **p. 2 / I. RELATION 70 Previous Work - extractive body cue:** Considerable attention is now directed toward the creation of efficient heuristic planners for many-dof robots.
- **p. 4 / A. The Learning Phase - extractive body cue:** The objective of the former is to obtain a reasonably connected ‘graph, with enough vertices to provide a rather uniform covering of free C-space and ...

## Design Rationale

- **p. 3 / I. RELATION 70 Previous Work - extractive body cue:** Our method emphasizes efficiency and is primarily developed for robots with many dofs which move in static ‘environments.
- **p. 1 / 1. IntRopuction - extractive body cue:** ‘We have demonstrated the power of our method by applying it to a number of difficult motion planning problems involving ‘variety of robots.
- **p. 1 / 1. IntRopuction - extractive body cue:** Moreover, increased efficiency can be achieved by tailoring several components of our method, in particular the local planner, to the considered robots.

## Source Evidence Cues

- **p. 7 / B. The Query Phase - extractive body cue:** An example of a planner having the above property is the straight-line planner of Section IIT-A-1, If the local planner performs a certain deterministic) action ...
- **p. 5 / 6) N.- a set of candidate neighbors - extractive body cue:** Firs, discretize the line segment (more ‘generally, any path generated by the local planner) into a number of configurations ¢1,...,¢m such that for each pair ...
- **p. 6 / 6) N.- a set of candidate neighbors - extractive body cue:** Then We try to connect n tothe other connected components of the network in the same way as in the constuction step.
- **p. 7 / B. The Query Phase - extractive body cue:** The local planner is deterministic and it will produce the same path every time it is called with the same input configurations Collisions need not ...
- **p. 8 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** First use the algorithm in [37] to produce the vertices of the obstacles in the link's C-space.
- **p. 8 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** (This algorithm takes linear time in the number of vertices of the objects.) Then draw and fill the obstacles into the 2-D bitmap.
- **p. 2 / I. RELATION 70 Previous Work - extractive body cue:** Considerable attention is now directed toward the creation of efficient heuristic planners for many-dof robots.
- **Detected method headings:** none reliably recovered

## Pipeline

| Module | Purpose | Input | Operation | Output | PDF cue | Anchor |
|---|---|---|---|---|---|---|
| Problem / state representation | decision state와 feasible set을 만든다 | state, map, goal, constraints | source-specific graph, symbolic state, belief 또는 configuration representation을 구성 | search/optimization state | An example of a planner having the above property is the straight-line planner of Section IIT-A-1, If the local planner performs a ... | p. 7 (B. The Query Phase), p. 5 (6) N.- a set of candidate neighbors) |
| Search / trajectory decision | goal을 향한 candidate를 생성·개선한다 | state와 cost/heuristic | search, sampling, dynamic programming 또는 trajectory optimization을 적용 | plan, path, option 또는 trajectory | Firs, discretize the line segment (more ‘generally, any path generated by the local planner) into a number of configurations ¢1,...,¢m such that ... | p. 5 (6) N.- a set of candidate neighbors), p. 6 (6) N.- a set of candidate neighbors) |
| Execution interface | 계획을 실행 가능한 command로 변환한다 | plan과 current feedback | collision/contact/dynamics check, smoothing, replanning 또는 controller handoff를 수행 | waypoint, option, action 또는 reference | Then We try to connect n tothe other connected components of the network in the same way as in the constuction step. | p. 6 (6) N.- a set of candidate neighbors), p. 7 (B. The Query Phase) |

- Pipeline rows are domain labels; the paper-specific operations are the extractive cues and section anchors in the same row.

## Objective / Update Rule

- **p. 4 / A. The Learning Phase - extractive body cue:** The objective of the former is to obtain a reasonably connected ‘graph, with enough vertices to provide a rather uniform covering of free C-space and ...
- **p. 5 / 6) N.- a set of candidate neighbors - extractive body cue:** The local planner wil be ealled to connect ¢ with nodes in N and the cumulative cost of these invocations dominates learning time.
- **p. 5 / 6) N.- a set of candidate neighbors - extractive body cue:** By considering elements of JV. in this order we ‘expect to maximize the chances of quickly connecting c to ‘other configurations and, consequently, reduce the ...
- **p. 8 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** It translates, ‘at constant relative velocity all the joints with an odd index, i.e, all Joei41°s, along the straight lines in the workspace that connect ...
- **p. 2 / I. RELATION 70 Previous Work - extractive body cue:** The main disadvantage of these planners is the presence of local minima in the potential fields.
- **p. 2 / I. RELATION 70 Previous Work - extractive body cue:** In these examples, RPP falls, into local minima whose basins of attraction are mostly bounded by obstacles, with only narrow passages to escape, ‘The probability ...
- **Formal bridge:** s/q -> a/ξ ∈ feasible decisions -> path/task cost or expected utility -> success/reachability and constraint satisfaction.
- **Equation/algorithm anchors:** p. 4 (A. The Learning Phase), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS), p. 4 (6) N.- a set of candidate neighbors), p. 5 (6) N.- a set of candidate neighbors), p. 5 (6) N.- a set of candidate neighbors).
- Do not infer optimizer, sign convention, target-network schedule, solver tolerance or stopping criterion unless the PDF states it.

## Variables and Parameters

| Role | PDF-derived terms | Normalized robotics interpretation | Status |
|---|---|---|---|
| Input/observation | During, query, phase, paths, found, between, arbitrary, input, start, goal, configurations, roadmap, constructed, learning | start/goal, map, dynamics와 successor/operator description | body cue; exact tensor/frame verify |
| State/latent | During, query, phase, paths, found, between, arbitrary, input, start, goal | path, trajectory, symbolic state 또는 task-motion decision | body cue; notation verify |
| Action/output | emphasizes, efficiency, primarily, developed, robots, many, dofs, move, static, environments | feasible action sequence 또는 minimum-cost plan | body cue; unit/decoder verify |
| Objective/constraint | objective, former, obtain, reasonably, connected, graph, enough, vertices, provide, rather | path/task cost or expected utility | equation anchor required |

## Observation–State–Action Interface

- **p. 6 / B. The Query Phase - extractive body cue:** During the query phase, paths are to be found between arbitrary input start and goal configurations, using the roadmap constructed in the learning phase.
- **p. 1 / Front matter - extractive body cue:** 6 IEEE TRANSACTIONS ON ROBOTICS AND AUTOMATION, VOL.
- **p. 1 / Abstract - extractive body cue:** Experimental results show that path planning can be done in a fraction of a second on a contemporary workstation (© 150 MIPS), after earning for ...
- **p. 2 / 1. IntRopuction - extractive body cue:** Once a good roadmap has been constructed, planning queries are processed in a fraction of a second
- **p. 2 / I. RELATION 70 Previous Work - extractive body cue:** In these examples, RPP falls, into local minima whose basins of attraction are mostly bounded by obstacles, with only narrow passages to escape, ‘The probability ...
- **p. 4 / I. RELATION 70 Previous Work - extractive body cue:** ‘In the query phase, the roadmap is used to solve individual path planning problems in the input scene.
- **p. 5 / 6) N.- a set of candidate neighbors - extractive body cue:** We actually tried several local planners, some very fast, some slower but more powerful, and our experimental observations clearly confirmed this conclusion eas see (40), ...
- **Normalized interface:** observation=start/goal, map, dynamics와 successor/operator description; state=path, trajectory, symbolic state 또는 task-motion decision; output/action=feasible action sequence 또는 minimum-cost plan.
- Verify whether output is directly actuated or passed through a planner, reference generator, controller, decoder or safety filter.

## Temporal and Runtime Contract

| Contract | Generic domain prior | PDF body cue | Unresolved detail |
|---|---|---|---|
| Horizon | start/goal 또는 task sequence까지의 long-horizon plan; exact horizon은 paper-specific. | Next, a graph search is done to find 4 sequence of edges connecting these nodes in the roadmap, Concatenation of the successive ... | episode/sequence/action-chunk boundary |
| Rate / latency | query/event-driven planning 뒤 controller가 partial plan을 실행; numeric rate 확인 필요. | In [21] [22] a sequential framework with backtracking is proposed for serial manipulators, and in, [14] a motion planner with performance proportional ... | Hz/fps, inference time and control rate |
| Memory | graph/tree/roadmap/plan and current state; history size는 method-specific. | not recovered | window and reset |
| Compute | collision checking, search branching 또는 optimization iterations가 latency를 결정한다. | not recovered | hardware, batch and throughput |

## Training vs Inference

- training/inference separation cue 없음

- Training inputs, privileged information, data augmentation and inference-time feedback must be recorded separately; they are not interchangeable.

## Method-Specific Formal Details

- **Body-defined terms:** example, planner, having, above, property, straight-line, Section, IIT-A-1, local, performs, certain, deterministic, action, when, collision, detected, then, collisions, need, checked.
- **Relevant PDF headings:** not reliably recovered.
- Exact equation text is not copied into the note; equation number, variable definition and role must be checked at the cited page.

## Evaluation Link

| Method component | Evaluation evidence to inspect | PDF anchor |
|---|---|---|
| Problem / state representation | Stil, the cases treated here are considerably easier than in the scenes of Section V, due 10, the relatively low number of ... | p. 12 (VI. RESULTS WITH GENERAL IMPLEMENTATION), p. 13 (VI. RESULTS WITH GENERAL IMPLEMENTATION) |
| Search / trajectory decision | Fig. 4. Results wih customized planner for scene of Fig. 2 (20 expansion) Pee. all / Sie of all chock for connection ... | p. 10 (Figure/Table caption), p. 12 (VI. RESULTS WITH GENERAL IMPLEMENTATION) |
| Execution interface | For each such pair of times we report the success rate in answering the query (s, 9). | p. 12 (VI. RESULTS WITH GENERAL IMPLEMENTATION), p. 12 (VI. RESULTS WITH GENERAL IMPLEMENTATION) |

## Failure and Ablation Link

- **p. 10 / Figure/Table caption - extractive body cue:** Fig. 4. Results wih customized planner for scene of Fig. 2 (20 expansion) Pee. all / Sie of all chock for connection to roadinap see) ...
- **p. 12 / VI. RESULTS WITH GENERAL IMPLEMENTATION - extractive body cue:** For each of these roadmaps we only consider its main connected component and we test whether the query with configurations (s,9) succeeds within 2.5 s.
- **p. 12 / VI. RESULTS WITH GENERAL IMPLEMENTATION - extractive body cue:** In other words, we test whether both s and g can be quickly connected to the main connected component of the roadmap with the method ...
- **p. 12 / VI. RESULTS WITH GENERAL IMPLEMENTATION - extractive body cue:** Unlike the ‘customized implementation, this implementation does not use any specific techniques for local path planning, collision ‘checking, or distance computation.
- **p. 13 / VI. Coxctusion - extractive body cue:** The collision checker in [44] considers successive approximations of the objects and, its running time, on the average, does not depend much on the
- **p. 11 / IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS - extractive body cue:** The actual number of collision checks for connecting C;,...,Cs of Fig.
- **p. 12 / VI. RESULTS WITH GENERAL IMPLEMENTATION - extractive body cue:** (1); and collision checking is done analytically, using routines from the PLAGEO library [19].

- Causal attribution requires fixing data, input modality, compute, horizon, action interface and controller while removing one component.

## Reproduction Checklist

1. [ ] Re-read the cited method headings and record exact variables, dimensions, units and equation/algorithm numbers.
2. [ ] Separate objective, constraint, initialization, update schedule and inference decoding.
3. [ ] Fix observation preprocessing, action frame, horizon, memory, rate and feedback latency.
4. [ ] Match dataset split, baseline checkpoint, seed/trial count and success denominator from 04.
5. [ ] Re-run the body-reported ablation and at least one failure-boundary stress test.

## Verification Questions

- **PDF anchors reviewed:** method p. 7 (B. The Query Phase), p. 5 (6) N.- a set of candidate neighbors), p. 6 (6) N.- a set of candidate neighbors), p. 7 (B. The Query Phase), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS), objective p. 4 (A. The Learning Phase), p. 5 (6) N.- a set of candidate neighbors), p. 5 (6) N.- a set of candidate neighbors), p. 8 (IV. APPLICATION 10 PLANAR ARTICULATED ROBOTS), p. 2 (I. RELATION 70 Previous Work), p. 2 (I. RELATION 70 Previous Work), temporal p. 1 (1. IntRopuction), p. 3 (I. RELATION 70 Previous Work), p. 4 (A. The Learning Phase), p. 4 (A. The Learning Phase), p. 5 (6) N.- a set of candidate neighbors), p. 6 (6) N.- a set of candidate neighbors).
- Which module is genuinely new, and which is inherited infrastructure or a baseline?
- What exact computation consumes each observation and emits each action/output?
- Does the reported runtime include preprocessing, planning, safety filtering and low-level control?
- Are all claims supported by a body section, equation, table or figure rather than the abstract alone?
