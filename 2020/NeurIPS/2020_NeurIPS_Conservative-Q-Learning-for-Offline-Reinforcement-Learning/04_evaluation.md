# Evaluation - Conservative Q-Learning for Offline Reinforcement Learning

> Canonical metadata: [01_overview.md](./01_overview.md).
> Evidence maturity: `FULL_TEXT_CHECKED`.
> Analysis basis: full-text PDF body checked on 2026-09-02 (31 pages; PyMuPDF text; extraction quality: high); canonical paper source: https://proceedings.neurips.cc/paper/2020/hash/0d2b2061826a5df3221116a5085a6052-Abstract.html; PDF retrieval source: https://arxiv.org/pdf/2006.04779. The note is an evidence-anchored body analysis; exact tables/equations remain at the cited page anchors. Reading tracker status/evidence was not changed.

## Evaluation in One Sentence

PDF body evaluation/result cue (p. 8 (Figure/Table caption), p. 31 (Figure/Table caption), p. 5 (2 Preliminaries), p. 8 (Figure/Table caption), p. 9 (Figure/Table caption), p. 9 (Figure/Table caption)): Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL is the only method that attains ...

## Evaluation Body Digest

- **p. 2 / 1 Introduction - extractive body cue:** CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning on ...
- **p. 1 / Abstract - extractive body cue:** Effectively leveraging large, previously collected datasets in reinforcement learning (RL) is a key challenge for large-scale real-world applications.
- **p. 1 / 1 Introduction - extractive body cue:** However, applying RL to real-world problems consistently poses practical challenges: in contrast to the kinds of data-driven methods that have been successful in supervised learning ...
- **p. 3 / 2 Preliminaries - extractive body cue:** Since standard Q-function training does not query the Q-function value at unobserved states, but queries the Q-function at unseen actions, we restrict µ to match ...
- **p. 2 / 2 Preliminaries - extractive body cue:** The dataset D is sampled from dπβ(s)πβ(a/s).
- **p. 3 / 2 Preliminaries - extractive body cue:** 3.1 Conservative Off-Policy Evaluation We aim to estimate the value V π(s) of a target policy π given access to a dataset, D, generated by ...
- **p. 5 / 2 Preliminaries - extractive body cue:** To begin with, we define empirical return of any policy π, J(π, ˆ M), which is equal to the discounted return of a policy π ...
- **p. 5 / 2 Preliminaries - extractive body cue:** This implies that the policy πk(a/s) ∝exp( ˆQk(s, a)), is constrained to be closer to the dataset distribution, ˆπβ(a/s), thus the CQL update implicitly prevents ...

## Evaluation Type and Scope

- **Evaluation type:** `EMPIRICAL / REAL-ROBOT OR HARDWARE`.
- **Target system/task:** offline robot transition/trajectory dataset과 deployment MDP.
- **Input boundary:** dataset state/observation, action, reward와 return-to-go.
- **Output/decision under evaluation:** dataset-supported action sequence.
- **Primary target:** offline policy value, OOD safety와 closed-loop success.
- **Detected evaluation headings:** not reliably recovered.

## Experimental Matrix

| Body section | Type | PDF experiment/result cue | Anchor |
|---|---|---|---|
| Figure/Table caption | EMPIRICAL / REAL-ROBOT OR HARDWARE | Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL ... | p. 8 (Figure/Table caption) |
| Figure/Table caption | EMPIRICAL / REAL-ROBOT OR HARDWARE | Table 6: Average return obtained by CQL(H) and CQL(H) without the dataset average Q-value maximization term. The latter formulation corresponds to Equation 1, which ... | p. 31 (Figure/Table caption) |
| 2 Preliminaries | EMPIRICAL / REAL-ROBOT OR HARDWARE | 3.3 Safe Policy Improvement Guarantees In Section 3.1 we proposed novel objectives for Q-function training such that the expected value of a policy under ... | p. 5 (2 Preliminaries) |
| Figure/Table caption | EMPIRICAL / REAL-ROBOT OR HARDWARE | Table 1: Performance of CQL(H) and prior methods on gym domains from D4RL, on the normalized return metric, averaged over 4 seeds. Note that ... | p. 8 (Figure/Table caption) |
| Figure/Table caption | EMPIRICAL / REAL-ROBOT OR HARDWARE | Table 3: CQL, REM and QR-DQN in setting (1) with 1% data (top), and 10% data (bottom). CQL drastically outperforms prior methods with 1% ... | p. 9 (Figure/Table caption) |

## Dataset / Benchmark Role

- **p. 2 / 1 Introduction - extractive body cue:** CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning on ...
- **p. 1 / Abstract - extractive body cue:** Effectively leveraging large, previously collected datasets in reinforcement learning (RL) is a key challenge for large-scale real-world applications.
- **p. 1 / 1 Introduction - extractive body cue:** However, applying RL to real-world problems consistently poses practical challenges: in contrast to the kinds of data-driven methods that have been successful in supervised learning ...
- **p. 3 / 2 Preliminaries - extractive body cue:** Since standard Q-function training does not query the Q-function value at unobserved states, but queries the Q-function at unseen actions, we restrict µ to match ...
- **p. 2 / 2 Preliminaries - extractive body cue:** The dataset D is sampled from dπβ(s)πβ(a/s).
- **p. 3 / 2 Preliminaries - extractive body cue:** 3.1 Conservative Off-Policy Evaluation We aim to estimate the value V π(s) of a target policy π given access to a dataset, D, generated by ...
- **p. 5 / 2 Preliminaries - extractive body cue:** To begin with, we define empirical return of any policy π, J(π, ˆ M), which is equal to the discounted return of a policy π ...
- **p. 5 / 2 Preliminaries - extractive body cue:** This implies that the policy πk(a/s) ∝exp( ˆQk(s, a)), is constrained to be closer to the dataset distribution, ˆπβ(a/s), thus the CQL update implicitly prevents ...

- Names are treated as evaluation resources only when the body sentence places them in a task, split, experiment or benchmark role.

## Figures / Tables as Body Evidence

- **p. 8 / Figure/Table caption - extractive body cue:** Table 1: Performance of CQL(H) and prior methods on gym domains from D4RL, on the normalized return metric, averaged over 4 seeds. Note that CQL ...
- **p. 8 / Figure/Table caption - extractive body cue:** Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL is ...
- **p. 9 / Figure/Table caption - extractive body cue:** Figure 1: Performance of CQL, QR-DQN and REM as a function of training steps (x-axis) in setting (1) when provided with only the first 20% ...
- **p. 9 / Figure/Table caption - extractive body cue:** Table 3: CQL, REM and QR-DQN in setting (1) with 1% data (top), and 10% data (bottom). CQL drastically outperforms prior methods with 1% data, ...
- **p. 9 / Figure/Table caption - extractive body cue:** Table 4: Difference between policy values predicted by each algorithm and the true policy value for CQL, a variant of CQL that uses Equation 1, ...
- **p. 16 / Figure/Table caption - extractive body cue:** Figure 2: ∆k as a function of training iterations for hopper-expert and hopper-medium datasets. Note that CQL (left) generally has negative values of ∆, whereas ...
- **p. 16 / Figure/Table caption - extractive body cue:** Figure 2. • Policy constraint methods, that do not impose any regularization on the Q-function may observe highly positive ˆ∆k values during training, especially with ...
- **p. 30 / Figure/Table caption - extractive body cue:** Table 5: Average return obtained by CQL(H), and CQL(ρ) on three D4RL MuJoCo environments. Observe that on these environments, CQL(H) generally outperforms CQL(ρ). Next, we ...

## Embodiment / Environment

| Dimension | PDF-grounded cue | Unresolved condition | Anchor |
|---|---|---|---|
| Robot/hardware/simulator | CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning ... | embodiment, simulator version and control stack | p. 2 (1 Introduction), p. 1 (Abstract) |
| Task/environment | Effectively leveraging large, previously collected datasets in reinforcement learning (RL) is a key challenge for large-scale real-world applications. | reset, timeout, object/scene variation | p. 1 (Abstract), p. 1 (1 Introduction) |
| Observation/sensor | dataset state/observation, action, reward와 return-to-go | calibration, preprocessing, privileged input | p. 2 (2 Preliminaries), p. 2 (2 Preliminaries) |
| Output/decision | dataset-supported action sequence | action frame, controller and termination | p. 3 (2 Preliminaries), p. 3 (2 Preliminaries) |

## Metrics and Success Definition

| Metric/result evidence | Definition and aggregation to verify | Anchor |
|---|---|---|
| We also empirically demonstrate the robustness of our approach to Q-function estimation error. | definition/direction/unit from same section | p. 2 (1 Introduction) |
| Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL ... | definition/direction/unit from same section | p. 8 (Figure/Table caption) |
| For clarity, we state the results in the absence of finitesample error, in this section, but sampling error can be incorporated in the same ... | definition/direction/unit from same section | p. 5 (2 Preliminaries) |
| (6) The expression of ζ in Theorem 3.6 consists of two terms: the first term captures the decrease in policy performance in M, that ... | definition/direction/unit from same section | p. 6 (2 Preliminaries) |
| Table 5: Average return obtained by CQL(H), and CQL(ρ) on three D4RL MuJoCo environments. Observe that on these environments, CQL(H) generally outperforms CQL(ρ). Next, ... | definition/direction/unit from same section | p. 30 (Figure/Table caption) |
| In practice, CQL augments the standard Bellman error objective with a simple Q-value regularizer which is straightforward to implement on top of existing deep ... | definition/direction/unit from same section | p. 1 (Abstract) |
| We assume that the rewards r satisfy: /r(s, a)/ ≤Rmax. | definition/direction/unit from same section | p. 2 (2 Preliminaries) |
| Following [49, 28, 47], we use concentration properties of ˆBπ to control this error. | definition/direction/unit from same section | p. 3 (2 Preliminaries) |

- Exact success denominator, collision/contact rule, timeout and primary-vs-auxiliary metric are recorded only when the body specifies them.

## Baselines and Fairness

| PDF baseline/comparison cue | Fair comparison field | Anchor |
|---|---|---|
| Table 5: Average return obtained by CQL(H), and CQL(ρ) on three D4RL MuJoCo environments. Observe that on these environments, CQL(H) generally outperforms CQL(ρ). Next, ... | comparison identity and matched condition | p. 30 (Figure/Table caption) |
| Table 6: Average return obtained by CQL(H) and CQL(H) without the dataset average Q-value maximization term. The latter formulation corresponds to Equation 1, which ... | comparison identity and matched condition | p. 31 (Figure/Table caption) |
| On both discrete and continuous control domains, we show that CQL substantially outperforms existing offline RL methods, often learning policies that attain 2-5 times ... | comparison identity and matched condition | p. 1 (Abstract) |
| CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning ... | comparison identity and matched condition | p. 2 (1 Introduction) |
| Table 1: Performance of CQL(H) and prior methods on gym domains from D4RL, on the normalized return metric, averaged over 4 seeds. Note that ... | comparison identity and matched condition | p. 8 (Figure/Table caption) |
| Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL ... | comparison identity and matched condition | p. 8 (Figure/Table caption) |

| Fairness dimension | Required matched condition |
|---|---|
| Observation/action | sensor modality, frame, preprocessing, action space and controller |
| Data | training split, demonstrations, pretraining, labels and leakage |
| Compute | parameter budget, inference steps, hardware, latency and control rate |
| Protocol | reset/timeout, seeds, trials, held-out variation and success denominator |

## Ablations and Sensitivity

| PDF ablation/sensitivity cue | What it isolates | Anchor |
|---|---|---|
| Table 4: Difference between policy values predicted by each algorithm and the true policy value for CQL, a variant of CQL that uses Equation ... | component/input/data sensitivity | p. 9 (Figure/Table caption) |
| Offline RL algorithms promise to learn effective policies from previously-collected, static datasets without further interaction. | component/input/data sensitivity | p. 1 (Abstract) |
| Frist, if ρ = Unif(a), then the first term in Equation 3 corresponds to a soft-maximum of the Q-values at any state s and ... | component/input/data sensitivity | p. 4 (2 Preliminaries) |
| In Appendix A, we discuss an additional variant of CQL, drawing connections to distributionally robust optimization [45]. | component/input/data sensitivity | p. 5 (2 Preliminaries) |
| find that this variant can be more stable with high-dimensional action spaces (e.g., Table 2) where it is challenging to estimate log P a ... | component/input/data sensitivity | p. 5 (2 Preliminaries) |
| 4 Practical Algorithm and Implementation Details Algorithm 1 Conservative Q-Learning (both variants) 1: Initialize Q-function, Qθ, and optionally a policy, πφ. | component/input/data sensitivity | p. 6 (2 Preliminaries) |

## Main Results / Claim–Evidence Map

| Claim or target | Result/condition cue | Evidence strength | Anchor |
|---|---|---|---|
| We propose a novel method for learning such conservative Qfunctions via a simple modification to standard value-based RL algorithms. | Table 2: Normalized scores of all methods on AntMaze, Adroit, and kitchen domains from D4RL, averaged across 4 seeds. On the harder mazes, CQL ... | PDF body cue; verify exact table/figure and matched conditions | p. 8 (Figure/Table caption), p. 31 (Figure/Table caption), p. 5 (2 Preliminaries), p. 8 (Figure/Table caption), p. 9 (Figure/Table caption), p. 9 (Figure/Table caption) |
| Primary metric/result | Table 6: Average return obtained by CQL(H) and CQL(H) without the dataset average Q-value maximization term. The latter formulation corresponds to Equation 1, which ... | numeric claim only at cited anchor | p. 31 (Figure/Table caption) |

- Numeric sentences retained from the body:
- **p. 2 / 1 Introduction - extractive body cue:** CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning on ...
- **p. 2 / 1 Introduction - extractive body cue:** CQL outperforms prior methods by as much as 2-5x on many benchmark tasks, and is the only method that can outperform simple behavioral cloning on ...

## Generalization and Failure Cases

| Body cue type | Observed cue or missing regime | Anchor |
|---|---|---|
| body limitation/failure cue | This has made current results fall short of the full promise of such methods. | p. 1 (1 Introduction) |
| body limitation/failure cue | In this paper, we propose conservative Q-learning (CQL), which aims to address these limitations by learning a conservative Q-function such that the expected value ... | p. 1 (Abstract) |
| body limitation/failure cue | Note that Q-function training in offline RL does not suffer from state distribution shift, as the Bellman backup never queries the Q-function on out-of-distribution ... | p. 2 (2 Preliminaries) |
| body limitation/failure cue | Figure 1: Performance of CQL, QR-DQN and REM as a function of training steps (x-axis) in setting (1) when provided with only the first ... | p. 9 (Figure/Table caption) |
| body limitation/failure cue | Since D typically does not contain all possible transitions (s, a, s′), the policy evaluation step actually uses an empirical Bellman operator that only ... | p. 2 (2 Preliminaries) |
| body limitation/failure cue | Since standard Q-function training does not query the Q-function value at unobserved states, but queries the Q-function at unseen actions, we restrict µ to ... | p. 3 (2 Preliminaries) |

- Do not treat unseen objects/tasks/scenes, sensor noise, contact changes, dynamic obstacles or recovery as evaluated unless the body reports them.

## Statistics, Efficiency, and Reproducibility

| Body reproducibility cue | Anchor |
|---|---|
| We use default hyperparameters from SAC, except that the learning rate for the policy is chosen to be 3e-5 (vs 3e-4 or 1e-4 for ... | p. 6 (2 Preliminaries) |
| Our algorithm requires an addition of only 20 lines of code on top of standard implementations of soft actor-critic (SAC) [21] for continuous control ... | p. 6 (2 Preliminaries) |
| In practice, CQL augments the standard Bellman error objective with a simple Q-value regularizer which is straightforward to implement on top of existing deep ... | p. 1 (Abstract) |
| However, applying RL to real-world problems consistently poses practical challenges: in contrast to the kinds of data-driven methods that have been successful in supervised ... | p. 1 (1 Introduction) |
| CQL can be implemented with less than 20 lines of code on top of a number of standard, online RL algorithms [21, 9], simply ... | p. 2 (1 Introduction) |

| Audit field | Current status |
|---|---|
| Trials/episodes/seeds | use only body sentences above; otherwise not reported |
| Mean/standard deviation/confidence interval | use only body table/figure; otherwise not reported |
| Latency/throughput | separate inference latency, control rate and simulator throughput |
| Train/eval split/leakage | verify dataset/protocol section |
| Code/checkpoint/environment | see 01_overview.md; not duplicated as evidence |

## Limitations and Verification Questions

- **p. 1 / 1 Introduction - extractive body cue:** This has made current results fall short of the full promise of such methods.
- **p. 1 / Abstract - extractive body cue:** In this paper, we propose conservative Q-learning (CQL), which aims to address these limitations by learning a conservative Q-function such that the expected value of ...
- **p. 2 / 2 Preliminaries - extractive body cue:** Note that Q-function training in offline RL does not suffer from state distribution shift, as the Bellman backup never queries the Q-function on out-of-distribution states.
- **p. 9 / Figure/Table caption - extractive body cue:** Figure 1: Performance of CQL, QR-DQN and REM as a function of training steps (x-axis) in setting (1) when provided with only the first 20% ...
- **p. 2 / 2 Preliminaries - extractive body cue:** Since D typically does not contain all possible transitions (s, a, s′), the policy evaluation step actually uses an empirical Bellman operator that only backs ...
- **p. 3 / 2 Preliminaries - extractive body cue:** Since standard Q-function training does not query the Q-function value at unobserved states, but queries the Q-function at unseen actions, we restrict µ to match ...

- **PDF anchors reviewed:** datasets p. 2 (1 Introduction), p. 1 (Abstract), p. 1 (1 Introduction), p. 3 (2 Preliminaries), p. 2 (2 Preliminaries), p. 3 (2 Preliminaries), metrics p. 2 (1 Introduction), p. 8 (Figure/Table caption), p. 5 (2 Preliminaries), p. 6 (2 Preliminaries), p. 30 (Figure/Table caption), p. 1 (Abstract), baselines p. 30 (Figure/Table caption), p. 31 (Figure/Table caption), p. 1 (Abstract), p. 2 (1 Introduction), p. 8 (Figure/Table caption), p. 8 (Figure/Table caption), results p. 8 (Figure/Table caption), p. 31 (Figure/Table caption), p. 5 (2 Preliminaries), p. 8 (Figure/Table caption), p. 9 (Figure/Table caption), p. 9 (Figure/Table caption).
- Which table/figure establishes the primary claim, and what is the exact denominator and uncertainty?
- Which comparison is fair after matching observation, data, compute, action interface, horizon and controller?
- What negative result or untested regime does the paper leave open?
