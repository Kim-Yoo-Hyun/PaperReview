# Group Equivariant Convolutional Networks

> Evidence maturity: `FULL_TEXT_CHECKED`.
> Analysis basis: full-text PDF body checked on 2026-09-03 (12 pages; PyMuPDF text; title-token overlap first two pages=1.0); canonical paper source: https://arxiv.org/abs/1602.07576.
> PDF retrieval source: https://arxiv.org/pdf/1602.07576. Reading tracker status remains user-controlled; registry source evidence is reconciled separately.

> Evidence boundary: selected PDF body sentences, captions and section anchors; exact table/equation values remain at those anchors.

- Year/Venue: 2016 / ICML
- Authors: not duplicated here when not verified in the registry source
- Primary track: Robotics-enabling 3D perception
- Tier: REFERENCE
- Tags: equivariant, representation, geometry
- Official paper: https://arxiv.org/abs/1602.07576
- Full-text retrieval: https://arxiv.org/pdf/1602.07576
- Code/Project: https://github.com/tscohen/gconv_experiments
- Paper type: method
- Source audit: full-text PDF body checked on 2026-09-03 (12 pages; PyMuPDF text; title-token overlap first two pages=1.0)

## Why This Paper Is Here

Robotics-enabling 3D perception의 3d_perception 문제를 이해하기 위해 읽는다. 본문은 Although a strong theory of neural network design is currently lacking, a large amount of empirical evidence supports the notion that both convolutional weight sharing and depth (among other factors) are important ...를 문제로 두고, Here we present the details for a G-convolution implementation that can leverage recent advances in fast computation of planar convolutions (Mathieu et al., 2014; Vasilache et al., 2015; Lavin & Gray, 2015).를 통해 observation-to-action closed loop의 한 지점을 바꾼다.

## Problem and Motivation

- **p. 1 / Abstract - extractive body cue:** We introduce Group equivariant Convolutional Neural Networks (G-CNNs), a natural generalization of convolutional neural networks that reduces sample complexity by exploiting symmetries.
- **p. 1 / Abstract - extractive body cue:** G-CNNs use G-convolutions, a new type of layer that enjoys a substantially higher degree of weight sharing than regular convolution layers.
- **p. 1 / Abstract - extractive body cue:** G-convolutions increase the expressive capacity of the network without increasing the number of parameters.
- **p. 1 / Abstract - extractive body cue:** Group convolution layers are easy to use and can be implemented with negligible computational overhead for discrete groups generated by translations, reflections and rotations.
- **p. 1 / Abstract - extractive body cue:** G-CNNs achieve state of the art results on CIFAR10 and rotated MNIST.
- **p. 1 / 1. Introduction - extractive body cue:** Although a strong theory of neural network design is currently lacking, a large amount of empirical evidence supports the notion that both convolutional weight sharing ...

## Core Idea

- **p. 6 / 7. Efficient Implementation - extractive body cue:** Here we present the details for a G-convolution implementation that can leverage recent advances in fast computation of planar convolutions (Mathieu et al., 2014; Vasilache ...
- **p. 6 / 7. Efficient Implementation - extractive body cue:** The computational cost of the algorithm presented here is roughly equal to that of a planar convolution with a filter bank that is the same ...
- **p. 7 / 7.2. Planar convolution - extractive body cue:** The second part of the G-convolution algorithm is a planar convolution using the expanded filter bank F +.
- **p. 7 / 7.1. Filter transformation - extractive body cue:** Group Equivariant Convolutional Networks channels at layer l, Sl-1 denotes the number of transformations in G that leave the origin invariant (e.g.

## Observation, State, and Output Interface

| Role | PDF body evidence | Robotics interpretation | Anchor |
|---|---|---|---|
| Observation/input | The resulting array can be interpreted as a conventional filter bank with Sl-1Kl-1 planar input channels and SlKl planar output channels, which can be correlated with the feature maps f (similarly reshaped). | RGB-D, image set, point cloud, depth와 camera pose | p. 7 (7.2. Planar convolution), p. 7 (7.1. Filter transformation) |
| State/latent | resulting, array, interpreted, conventional, filter, bank, Sl-1Kl-1, planar, input, channels, SlKl, output | geometry, map, object/relationship state | p. 7 (7.2. Planar convolution), p. 7 (7.1. Filter transformation), p. 1 (1. Introduction) |
| Output/action | To precompute the indices, we define an invertible map g(s, u, v) that takes an input index (valid for an array of shape Sl-1 × n × n) and produces the associated ... | point map, pose, scene graph, affordance 또는 query result | p. 7 (7.1. Filter transformation), p. 1 (1. Introduction), p. 1 (1. Introduction) |
| Objective/outcome | The computational cost of the algorithm presented here is roughly equal to that of a planar convolution with a filter bank that is the same size as the augmented filter bank used ... | geometric accuracy, semantic consistency와 planning/manipulation utility | p. 6 (7. Efficient Implementation) |

## Main Claims and Actual Contribution

- **p. 6 / 7. Efficient Implementation - extractive body cue:** Here we present the details for a G-convolution implementation that can leverage recent advances in fast computation of planar convolutions (Mathieu et al., 2014; Vasilache ...
- **p. 7 / 8.1. Rotated MNIST - extractive body cue:** This network (P4CNNRotationPooling) outperforms the baseline and the previous state of the art, but performs significantly worse than the P4CNN which does not pool over ...
- **p. 8 / 8.1. Rotated MNIST - extractive body cue:** To the best of our knowledge, the p4m-CNN outperforms all published results on plain CIFAR10 (Wan et al., 2013; Goodfellow et al., 2013; Lin et ...
- **p. 7 / 8.1. Rotated MNIST - extractive body cue:** This baseline architecture outperforms the models tested by Larochelle et al.
- **p. 8 / 8.1. Rotated MNIST - extractive body cue:** Extreme data augmentation and model ensembles can also further improve the numbers (Graham, 2014).

- Claims are retained as body cues; exact percentages and table values must be read at the cited result anchor.

## Evaluation Scope

| Dimension | Body-grounded record | Boundary | Anchor |
|---|---|---|---|
| Evaluation type | SYSTEM / EVALUATION SCOPE UNRESOLVED | do not infer unreported downstream behavior | p. 7 (8.1. Rotated MNIST), p. 8 (8.1. Rotated MNIST) |
| Embodiment/environment | The dataset is split into a training, validation and test sets of size 10000, 2000 and 50000, respectively. | hardware/simulator version and reset protocol | p. 7 (8.1. Rotated MNIST), p. 7 (8.1. Rotated MNIST) |
| Dataset/benchmark | Test set error rates and number of parameters are reported. | role, split, size and leakage | p. 7 (8.1. Rotated MNIST), p. 7 (8.1. Rotated MNIST), p. 8 (8.1. Rotated MNIST) |
| Metric | Error rates on rotated MNIST (with standard deviation under variation of the random seed). | definition, denominator, direction and uncertainty | p. 7 (8.1. Rotated MNIST), p. 7 (8.1. Rotated MNIST), p. 8 (8.1. Rotated MNIST) |
| Baseline/ablation | This baseline architecture outperforms the models tested by Larochelle et al. | fair input/data/compute/action matching | p. 7 (8.1. Rotated MNIST), p. 7 (8.1. Rotated MNIST), p. 8 (8.1. Rotated MNIST) |

## Explicit Limitations and Failure Boundary

- **p. 8 / 9. Discussion & Future work - extractive body cue:** One limitation of the method as presented here is that it only works for discrete groups.
- **p. 8 / 9. Discussion & Future work - extractive body cue:** In future work, we want to implement G-CNNs that work on hexagonal lattices which have an increased number of symmetries relative to square grids, as ...
- **p. 7 / 8.1. Rotated MNIST - extractive body cue:** (2007) (when trained on 12k and evaluated on 50k), but does not match the previous state of the art, which uses prior knowledge about rotations ...
- **p. 7 / 8.1. Rotated MNIST - extractive body cue:** This network (P4CNNRotationPooling) outperforms the baseline and the previous state of the art, but performs significantly worse than the P4CNN which does not pool over ...

## Why Read It

Robotics-enabling 3D perception의 3d_perception 문제를 이해하기 위해 읽는다. 본문은 Although a strong theory of neural network design is currently lacking, a large amount of empirical evidence supports the notion that both convolutional weight sharing and depth (among other factors) are important ...를 문제로 두고, Here we present the details for a G-convolution implementation that can leverage recent advances in fast computation of planar convolutions (Mathieu et al., 2014; Vasilache et al., 2015; Lavin & Gray, 2015).를 통해 observation-to-action closed loop의 한 지점을 바꾼다. Revisit p. 1 (1. Introduction), p. 6 (7. Efficient Implementation), p. 7 (7.2. Planar convolution), p. 7 (7.1. Filter transformation), p. 7 (8.1. Rotated MNIST), p. 8 (8.1. Rotated MNIST) to check whether the claimed mechanism survives the failure regime and evaluation boundary recorded above.
