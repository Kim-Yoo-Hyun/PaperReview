# Evaluation - Scalable Non-Equivariant 3D Molecule Generation via Rotational Alignment

> Canonical metadata: [01_overview.md](./01_overview.md).
> Evidence maturity: `FULL_TEXT_CHECKED`.
> Analysis basis: full-text PDF body checked on 2026-09-01 (11 pages; PyMuPDF text; extraction quality: high); canonical paper source: https://openreview.net/forum?id=l5KpQ5MmaD; PDF retrieval source: https://openreview.net/pdf/d526a2c92e7570f45137984e599cd180fcdcf5b6.pdf. The note is an evidence-anchored body analysis; exact tables/equations remain at the cited page anchors. Reading tracker status/evidence was not changed.

## Evaluation in One Sentence

PDF body evaluation/result cue (p. 7 (Figure/Table caption), p. 6 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup), p. 7 (4.1. Experimental Setup), p. 8 (4.5. Efficiency Comparison), p. 8 (4.6. Conditional Generation)): Figure 1: Molecules generated by RADMDiT-B on QM9 (the three on the left) and GEOM-Drugs (the three on the right). non-equivariant models. RADMDiT-S outperforms EDM on molecule stability and outperforms ...

## Evaluation Body Digest

- **p. 5 / 4.1. Experimental Setup - extractive PDF cue:** Datasets We first evaluate our approach using the QM9 dataset (Ramakrishnan et al., 2014) which is a standard molecule generation benchmark widely used by related ...
- **p. 5 / 4.1. Experimental Setup - extractive PDF cue:** We split the dataset in the same way as Hoogeboom et al.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** For both datasets, our model learns to generate the 3D coordinates and atom types of complete molecules with explicit hydrogens.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** We use the same hidden dimension and number of layers for the autoencoder as GeoLDM, and the number of layers of the rotation network is ...
- **p. 7 / 4.3. GEOM-Drugs - extractive PDF cue:** From the table we observe that RADM exhibits very strong performance on this much larger dataset with more complex molecules.
- **p. 8 / 4.6. Conditional Generation - extractive PDF cue:** The QM9 training set is equally divided into two parts.
- **p. 8 / 4.6. Conditional Generation - extractive PDF cue:** The numbers for "QM9" are obtained by evaluating the property predictor directly on the second half of the training set, which can be considered as ...
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** We report the average performance and standard deviation across three runs, each sampling 10000 molecules.

## Evaluation Type and Scope

- **Evaluation type:** `EMPIRICAL / SOURCE-REPORTED EVALUATION`.
- **Target system/task:** 3D scene/object와 robot coordinate frame.
- **Input boundary:** RGB-D, image set, point cloud, depth와 camera pose.
- **Output/decision under evaluation:** point map, pose, scene graph, affordance 또는 query result.
- **Primary target:** geometric accuracy, semantic consistency와 planning/manipulation utility.
- **Detected evaluation headings:** 4. Experiments (p. 5); 4.1. Experimental Setup (p. 5).

## Experimental Matrix

| Body section | Type | PDF experiment/result cue | Anchor |
|---|---|---|---|
| Figure/Table caption | EMPIRICAL / SOURCE-REPORTED EVALUATION | Figure 1: Molecules generated by RADMDiT-B on QM9 (the three on the left) and GEOM-Drugs (the three on the right). non-equivariant models. RADMDiT-S outperforms ... | p. 7 (Figure/Table caption) |
| 4.1. Experimental Setup | EMPIRICAL / SOURCE-REPORTED EVALUATION | As we can see from the table, diffusion models perform much better than ENF and G-SchNet, and equivariant baselines significantly outperform non-equivariant baselines. | p. 6 (4.1. Experimental Setup) |
| 4.1. Experimental Setup | EMPIRICAL / SOURCE-REPORTED EVALUATION | Notably, both RADMDiT-S and RADMDiT-B improve the performance drastically compared with previous 6 | p. 6 (4.1. Experimental Setup) |
| 4.1. Experimental Setup | EMPIRICAL / SOURCE-REPORTED EVALUATION | We note that most hyper-parameters used in our experiments simply follow the equivariant baselines (GeoLDM and EDM), and more tuning may further improve the ... | p. 7 (4.1. Experimental Setup) |
| 4.5. Efficiency Comparison | EMPIRICAL / SOURCE-REPORTED EVALUATION | These results demonstrate that our non-equivariant model has better scalability and great potential for further improvement. | p. 8 (4.5. Efficiency Comparison) |

## Dataset / Benchmark Role

- **p. 5 / 4.1. Experimental Setup - extractive PDF cue:** Datasets We first evaluate our approach using the QM9 dataset (Ramakrishnan et al., 2014) which is a standard molecule generation benchmark widely used by related ...
- **p. 5 / 4.1. Experimental Setup - extractive PDF cue:** We split the dataset in the same way as Hoogeboom et al.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** For both datasets, our model learns to generate the 3D coordinates and atom types of complete molecules with explicit hydrogens.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** We use the same hidden dimension and number of layers for the autoencoder as GeoLDM, and the number of layers of the rotation network is ...
- **p. 7 / 4.3. GEOM-Drugs - extractive PDF cue:** From the table we observe that RADM exhibits very strong performance on this much larger dataset with more complex molecules.
- **p. 8 / 4.6. Conditional Generation - extractive PDF cue:** The QM9 training set is equally divided into two parts.
- **p. 8 / 4.6. Conditional Generation - extractive PDF cue:** The numbers for "QM9" are obtained by evaluating the property predictor directly on the second half of the training set, which can be considered as ...

- Names are treated as evaluation resources only when the body sentence places them in a task, split, experiment or benchmark role.

## Figures / Tables as Body Evidence

- **p. 6 / Figure/Table caption - extractive PDF cue:** Table 1: Results of atom stability, molecule stability, validity and validity × uniqueness. Higher numbers are better. We use distinct colors to indicate the best ...
- **p. 7 / Figure/Table caption - extractive PDF cue:** Figure 1: Molecules generated by RADMDiT-B on QM9 (the three on the left) and GEOM-Drugs (the three on the right). non-equivariant models. RADMDiT-S outperforms EDM ...
- **p. 7 / Figure/Table caption - extractive PDF cue:** Table 2: Results (as percentages) of the ablation study on QM9 with the same diffusion backbone. Atom Sta Mol Sta Valid GraphLDM 97.2 70.5
- **p. 7 / Figure/Table caption - extractive PDF cue:** Table 3: Comparison of model size, average training time per epoch and sampling efficiency. #Params Training Sampling EDM 5.3M 107.9s
- **p. 8 / Figure/Table caption - extractive PDF cue:** Table 4: Mean Absolute Error for molecular property predic- tion by a pretrained predictor (lower is better). The baseline performance is borrowed from EDM and ...
- **p. 11 / Figure/Table caption - extractive PDF cue:** Figure 2: Molecule samples from the training set.
- **p. 11 / Figure/Table caption - extractive PDF cue:** Figure 3: Molecule samples after applying the learned rotations.

## Embodiment / Environment

| Dimension | PDF-grounded cue | Unresolved condition | Anchor |
|---|---|---|---|
| Robot/hardware/simulator | Datasets We first evaluate our approach using the QM9 dataset (Ramakrishnan et al., 2014) which is a standard molecule generation benchmark widely used by ... | embodiment, simulator version and control stack | p. 5 (4.1. Experimental Setup), p. 5 (4.1. Experimental Setup) |
| Task/environment | We split the dataset in the same way as Hoogeboom et al. | reset, timeout, object/scene variation | p. 5 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup) |
| Observation/sensor | RGB-D, image set, point cloud, depth와 camera pose | calibration, preprocessing, privileged input | p. 3 (2.3. Equivariance), p. 4 (3.1. Aligned Latent Space) |
| Output/decision | point map, pose, scene graph, affordance 또는 query result | action frame, controller and termination | p. 2 (2.2. Diffusion Models), p. 3 (2.2. Diffusion Models) |

## Metrics and Success Definition

| Metric/result evidence | Definition and aggregation to verify | Anchor |
|---|---|---|
| We report the average performance and standard deviation across three runs, each sampling 10000 molecules. | definition/direction/unit from same section | p. 6 (4.1. Experimental Setup) |
| Table 4: Mean Absolute Error for molecular property predic- tion by a pretrained predictor (lower is better). The baseline performance is borrowed from EDM ... | definition/direction/unit from same section | p. 8 (Figure/Table caption) |
| In Section 4.5, we demonstrate the efficiency and scalability of our non-equivariant model. | definition/direction/unit from same section | p. 5 (4. Experiments) |
| QM9 We train our model to generate molecules unconditionally. | definition/direction/unit from same section | p. 6 (4.1. Experimental Setup) |
| The sampling speed is measured by the average time used to generate 100 samples 7 | definition/direction/unit from same section | p. 7 (4.5. Efficiency Comparison) |
| We find that scaling from DiT-S to DiT-B consistently boosts performance on all metrics. | definition/direction/unit from same section | p. 7 (4.1. Experimental Setup) |
| In this section, we evaluate the ability of our model to generate molecules with desired properties. | definition/direction/unit from same section | p. 8 (4.6. Conditional Generation) |

- Exact success denominator, collision/contact rule, timeout and primary-vs-auxiliary metric are recorded only when the body specifies them.

## Baselines and Fairness

| PDF baseline/comparison cue | Fair comparison field | Anchor |
|---|---|---|
| As we can see from the table, diffusion models perform much better than ENF and G-SchNet, and equivariant baselines significantly outperform non-equivariant baselines. | comparison identity and matched condition | p. 6 (4.1. Experimental Setup) |
| The nonequivariant baselines GraphLDM and GraphLDM-aug used the same GNN architecture as the noise prediction network, but were trained in a latent space without ... | comparison identity and matched condition | p. 7 (4.4. Ablation Study) |
| In Section 4.1, we introduce the experimental setup, including the datasets, baselines and implementation details. | comparison identity and matched condition | p. 5 (4. Experiments) |
| Baseline results are copied from respective papers. | comparison identity and matched condition | p. 6 (4.1. Experimental Setup) |
| RADMDiT-S is already highly competitive compared with the best equivariant model GeoLDM. | comparison identity and matched condition | p. 7 (4.3. GEOM-Drugs) |
| The baseline performance is borrowed from EDM and GeoLDM. | comparison identity and matched condition | p. 8 (4.5. Efficiency Comparison) |

| Fairness dimension | Required matched condition |
|---|---|
| Observation/action | sensor modality, frame, preprocessing, action space and controller |
| Data | training split, demonstrations, pretraining, labels and leakage |
| Compute | parameter budget, inference steps, hardware, latency and control rate |
| Protocol | reset/timeout, seeds, trials, held-out variation and success denominator |

## Ablations and Sensitivity

| PDF ablation/sensitivity cue | What it isolates | Anchor |
|---|---|---|
| The nonequivariant baselines GraphLDM and GraphLDM-aug used the same GNN architecture as the noise prediction network, but were trained in a latent space without ... | component/input/data sensitivity | p. 7 (4.4. Ablation Study) |
| To validate the effectiveness of the alignment itself, we conduct an ablation study using the same architecture for the diffusion model. | component/input/data sensitivity | p. 7 (4.4. Ablation Study) |
| In Section 4.4, we show the results of ablation studies. | component/input/data sensitivity | p. 5 (4. Experiments) |
| In Section 4.5, we demonstrate the efficiency and scalability of our non-equivariant model. | component/input/data sensitivity | p. 5 (4. Experiments) |
| Among them, EDM and GeoLDM are state-of-the-art equivariant diffusion models. | component/input/data sensitivity | p. 6 (4.1. Experimental Setup) |
| Similarly, we compare with GeoLDM's non-equivariant versions GraphLDM and GraphLDM-aug. | component/input/data sensitivity | p. 6 (4.1. Experimental Setup) |

## Main Results / Claim–Evidence Map

| Claim or target | Result/condition cue | Evidence strength | Anchor |
|---|---|---|---|
| To address this challenge, we propose to construct aligned representations in an unsupervised manner with an autoencoder. | Figure 1: Molecules generated by RADMDiT-B on QM9 (the three on the left) and GEOM-Drugs (the three on the right). non-equivariant models. RADMDiT-S outperforms ... | PDF body cue; verify exact table/figure and matched conditions | p. 7 (Figure/Table caption), p. 6 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup), p. 7 (4.1. Experimental Setup), p. 8 (4.5. Efficiency Comparison), p. 8 (4.6. Conditional Generation) |
| Primary metric/result | As we can see from the table, diffusion models perform much better than ENF and G-SchNet, and equivariant baselines significantly outperform non-equivariant baselines. | numeric claim only at cited anchor | p. 6 (4.1. Experimental Setup) |

- Numeric sentences retained from the body:
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** QM9 GEOM-Drugs Atom Sta (%) Mol Sta (%) Valid (%) Valid & Unique (%) Atom Sta (%) Valid (%) Data 99.0 95.2 97.7 97.7 86.5 ...
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** We train the autoencoder (and the rotation network) using the Adam optimizer with a learning rate of 1×10-4 and a cosine annealing schedule.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** On QM9, we train the autoencoder for 200 epochs using a batch size of 64.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** We adopt a batch size of 256 as used in the DiT paper, and train both RADMDiT-S and RADMDiT-B for around 5500 epochs.
- **p. 7 / 4.3. GEOM-Drugs - extractive PDF cue:** On GEOM-Drugs, the autoencoder is trained for 4 epochs using a batch size of 32.
- **p. 7 / 4.3. GEOM-Drugs - extractive PDF cue:** Then we train RADMDiT-S and RADMDiT-B for around 55 epochs with a batch size of 256.

## Generalization and Failure Cases

| Body cue type | Observed cue or missing regime | Anchor |
|---|---|---|
| body limitation/failure cue | As for the diffusion model, we use the same noise schedule and number of time steps as EDM/GeoLDM. | p. 6 (4.1. Experimental Setup) |
| body limitation/failure cue | To implement DiT as the noise prediction network, we follow the official code base6 and make necessary modifications as explained in Section 3.2. | p. 6 (4.1. Experimental Setup) |
| body limitation/failure cue | Specifically, we use a basic non-equivariant GNN (17) as the noise prediction network and train the diffusion model based on the same trained autoencoder ... | p. 7 (4.4. Ablation Study) |
| body limitation/failure cue | The nonequivariant baselines GraphLDM and GraphLDM-aug used the same GNN architecture as the noise prediction network, but were trained in a latent space without ... | p. 7 (4.4. Ablation Study) |
| body limitation/failure cue | (2021) derived a continuous-time variational lower bound for diffusion models by considering infinitely deep VAEs and simplified the expression in terms of the signal-to-noise ... | p. 8 (5.1. Diffusion Models) |
| body limitation/failure cue | DDPM (Ho et al., 2020) established a connection between diffusion models and denoising score matching and derived a simple training objective via parameterizing a ... | p. 8 (5.1. Diffusion Models) |

- Do not treat unseen objects/tasks/scenes, sensor noise, contact changes, dynamic obstacles or recovery as evaluated unless the body reports them.

## Statistics, Efficiency, and Reproducibility

| Body reproducibility cue | Anchor |
|---|---|
| On QM9, we train the autoencoder for 200 epochs using a batch size of 64. | p. 6 (4.1. Experimental Setup) |
| We adopt a batch size of 256 as used in the DiT paper, and train both RADMDiT-S and RADMDiT-B for around 5500 epochs. | p. 6 (4.1. Experimental Setup) |
| We add the number of parameters of the autoencoder but exclude its training time, since training the autoencoder takes only a small portion of ... | p. 7 (4.5. Efficiency Comparison) |
| We list the parameter counts, average training time per epoch and sampling speed in Table 3. | p. 7 (4.5. Efficiency Comparison) |
| In Section 4.1, we introduce the experimental setup, including the datasets, baselines and implementation details. | p. 5 (4. Experiments) |
| Scalable Non-Equivariant 3D Molecule Generation via Rotational Alignment as one mini-batch, and we use 1000 sampling steps for all models. | p. 8 (4.5. Efficiency Comparison) |
| Specifically, our conditional model uses the same pretrained autoencoder as in Section 4.2, but trains the noise prediction network of the latent diffusion model ... | p. 8 (4.6. Conditional Generation) |
| Given the distributions above and using Bayes' rule, we can derive the true posterior distribution of the forward transition (4), conditioned on x: q(zs/zt, ... | p. 2 (2.2. Diffusion Models) |

| Audit field | Current status |
|---|---|
| Trials/episodes/seeds | use only body sentences above; otherwise not reported |
| Mean/standard deviation/confidence interval | use only body table/figure; otherwise not reported |
| Latency/throughput | separate inference latency, control rate and simulator throughput |
| Train/eval split/leakage | verify dataset/protocol section |
| Code/checkpoint/environment | see 01_overview.md; not duplicated as evidence |

## Limitations and Verification Questions

- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** As for the diffusion model, we use the same noise schedule and number of time steps as EDM/GeoLDM.
- **p. 6 / 4.1. Experimental Setup - extractive PDF cue:** To implement DiT as the noise prediction network, we follow the official code base6 and make necessary modifications as explained in Section 3.2.
- **p. 7 / 4.4. Ablation Study - extractive PDF cue:** Specifically, we use a basic non-equivariant GNN (17) as the noise prediction network and train the diffusion model based on the same trained autoencoder as ...
- **p. 7 / 4.4. Ablation Study - extractive PDF cue:** The nonequivariant baselines GraphLDM and GraphLDM-aug used the same GNN architecture as the noise prediction network, but were trained in a latent space without learned ...
- **p. 8 / 5.1. Diffusion Models - extractive PDF cue:** (2021) derived a continuous-time variational lower bound for diffusion models by considering infinitely deep VAEs and simplified the expression in terms of the signal-to-noise ratio.
- **p. 8 / 5.1. Diffusion Models - extractive PDF cue:** DDPM (Ho et al., 2020) established a connection between diffusion models and denoising score matching and derived a simple training objective via parameterizing a noise ...

- **PDF anchors reviewed:** datasets p. 5 (4.1. Experimental Setup), p. 5 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup), p. 7 (4.3. GEOM-Drugs), p. 8 (4.6. Conditional Generation), metrics p. 6 (4.1. Experimental Setup), p. 8 (Figure/Table caption), p. 5 (4. Experiments), p. 6 (4.1. Experimental Setup), p. 7 (4.5. Efficiency Comparison), p. 7 (4.1. Experimental Setup), baselines p. 6 (4.1. Experimental Setup), p. 7 (4.4. Ablation Study), p. 5 (4. Experiments), p. 6 (4.1. Experimental Setup), p. 7 (4.3. GEOM-Drugs), p. 8 (4.5. Efficiency Comparison), results p. 7 (Figure/Table caption), p. 6 (4.1. Experimental Setup), p. 6 (4.1. Experimental Setup), p. 7 (4.1. Experimental Setup), p. 8 (4.5. Efficiency Comparison), p. 8 (4.6. Conditional Generation).
- Which table/figure establishes the primary claim, and what is the exact denominator and uncertainty?
- Which comparison is fair after matching observation, data, compute, action interface, horizon and controller?
- What negative result or untested regime does the paper leave open?
